{
  "name": "scraper",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx --env-file-if-exists=.env src/index.ts",
    "seed-dev": "tsx --env-file-if-exists=.env src/seed-dev.ts",
    "check": "tsx --env-file-if-exists=.env src/check.ts",
    "check-freshness": "tsx --env-file-if-exists=.env src/check-freshness.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.8"
  }
}
